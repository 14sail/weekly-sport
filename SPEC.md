# 共用規範

三個頁面（`index.html` 運動排程、`trip.html` 旅程行程、`split.html` 團體記帳）
是同一個產品的三個入口，姊妹們會在它們之間來回切換。**任何共用的東西改了一處，
就要三處都改**，否則切過去馬上看得出來不一樣。

這份文件就是「共用的東西」的清單。動任何一項之前先看這裡。

---

## 1. 改動流程

```
test/<檔案>  →  驗證  →  <檔案>（正式站）
```

- 一律先改 `test/` 底下的複本，推上去在測試網址驗證，再搬到正式站。
- 搬移時**只有 `firebaseConfig` 那一段不能覆蓋**（正式站指向 `weekly-sport`，
  測試站指向 `weekly-sport-test`）。搬完必須確認：
  - `grep -c 'weekly-sport-test' <正式檔>` 要等於 `0`
  - `diff test/<檔案> <檔案>` 的差異行數要剛好是 14（設定的 7 行 × 兩邊）
- 推之前跑一次語法檢查：把 `<script>` 到 `</script>` 之間抽出來 `node --check`。

---

## 2. 設計 token

三個檔案的 `:root` 必須一致。改了顏色就是三個檔案一起改。

| token | 值 | 用途 |
|---|---|---|
| `--bg` | `#F6F1E7` | 頁面底色；也是「已結束」卡片的底色 |
| `--bg-card` | `#FFFDF8` | 卡片底色 |
| `--ink` | `#3A2E22` | 主要文字 |
| `--ink-soft` | `#796B58` | 次要文字。**不可以再調淡**，這個值是為了讓 12–13px 的小字在兩種底色上都達到 4.5:1 才定出來的 |
| `--line` | `#E4D9C4` | 一般框線 |
| `--line-soft` | `#DFD7C9` | 更淺的分隔線 |
| `--accent` | `#A9744F` | 主色 |
| `--accent-deep` | `#7C5236` | 主色深，用於文字與焦點外框 |
| `--accent-soft` | `#EFE0CB` | 主色淺底，用於選中與按下狀態 |
| `--radius` / `--radius-sm` | `16px` / `10px` | 圓角 |

疊層順序也集中在 `:root`，不要在各處自己填數字：

```
--z-stick: 20    釘在上緣的分頁／日期列
--z-corner: 30   右上角說明鈕（必須高於釘住的列，否則捲動時會被蓋掉）
--z-mask: 40     面板遮罩
--z-sheet: 41    貼底面板
--z-help: 50     說明書
```

### 對比是硬規定

小字（<18px）對背景要 **≥ 4.5:1**。改任何顏色之前先算，不要憑感覺。

#### 語意色有兩層，不要拿錯

`--red` / `--green` / `--amber` 是照「放在 `--bg-card` 上」調的，**放到自己的 `-soft`
淺底上會不及格**。文字疊在 `-soft` 底色上一律用 `-ink` 那一組。

| 用途 | token | 對比 |
|---|---|---:|
| 文字放在 `--red-soft` 上 | `--red-ink` `#A04E3D` | 4.58 ✓ |
| 文字放在 `--green-soft` 上 | `--green-ink` `#5E6B46` | 4.72 ✓ |
| 文字放在 `--amber-soft` 上 | `--amber-ink` `#8A6220` | 4.57 ✓ |
| ~~`--red` 放在 `--red-soft` 上~~ | | 3.72 ✗ |
| ~~`--green` 放在 `--green-soft` 上~~ | | 3.81 ✗ |
| ~~`--amber` 放在 `--amber-soft` 上~~ | | 2.54 ✗ |

`--red` / `--green` / `--amber` 繼續用在實心底色、框線，以及放在 `--bg` / `--bg-card`
上的文字（4.15 / 4.11 / **2.69 ✗**——`--amber` 當文字在哪裡都不及格，一律改用
`--amber-ink`）。

**白字只能放在這些底色上**（算過的）：

| 底色 | 白字對比 | 可用 |
|---|---:|---|
| `--accent-deep` `#7C5236` | 6.74 | ✓ |
| `--red` `#B15C4A` | 4.67 | ✓ |
| `--green` `#6C7A52` | 4.62 | ✓ |
| `--accent` `#A9744F` | 3.96 | ✗ |
| `--amber` `#C08A34` | 3.03 | ✗ |

**實心的主色按鈕、選中的分頁、選中的日期鈕，底色一律用 `--accent-deep`**。
`--accent` 只能當框線、淺色底的搭配色，或不放白字的地方。

其他已知會踩到的陷阱：

- **不要用 `opacity` 表示「停用／已結束」狀態**。整塊變淡會連文字一起淡掉，
  對比會掉到 3 以下。改用底色區分（`background:var(--bg)`）。
- `opacity` 只有真正 `disabled` 的控制項可以用（WCAG 明文豁免）。
- **焦點外框不可以用淺色。** `--accent-soft` 對 `--bg-card` 只有 1.28:1，拿它當
  `outline` 等於把瀏覽器原本看得見的預設外框換成看不見的，比不寫還糟。
  三站的 `input:focus` 與 `button:focus-visible` 一律用 `--accent-deep`（6.63:1）。
- **錯誤與成功訊息要加 `role="alert"`**，否則讀螢幕的人完全收不到。畫面上看不出差別，
  所以很容易被漏掉。

---

## 3. 觸控與互動（三站一致）

| 規則 | 做法 |
|---|---|
| 觸控尺寸 | 可點的東西 **≥ 44×44px**。視覺上要小的按鈕用 `::after { inset:-4px }` 把感應範圍撐開，不要真的把按鈕做大 |
| 按鈕間距 | `gap: 8px`，剛好是感應範圍外擴值的兩倍，相鄰按鈕才不會互搶 |
| 輸入框 | `min-height: 44px`。**核取方塊要排除**（`input[type=checkbox]{min-height:0}`），否則會被撐成大方塊；觸控範圍改由包住它的整列提供（`.chk-row` / `.split-row`，整列 `min-height:44px`） |
| 按下回饋 | 每個可點元素都要有 `:active` 變色。**一律用顏色，不要用會改變版面的 transform** |
| iOS 的坑 | iOS Safari 只有在 document 有 touch 監聽時才會套用 `:active`。每個檔案都要有一行 `document.addEventListener('touchstart', function(){}, {passive:true})`，否則所有按下效果在 iPhone 上都不會出現 |
| 點擊延遲 | `button, a, label, summary { touch-action: manipulation }` |
| 焦點 | `button:focus-visible` 等要有外框。用 `:focus-visible` 不是 `:focus`，滑鼠點按才不會跳框 |
| 減少動態 | 每個檔案都要有 `@media (prefers-reduced-motion: reduce)` 區塊 |
| 安全區 | 固定定位的元素要算進 `env(safe-area-inset-*)`，橫向使用時瀏海會吃掉右上角 |

### 已知的偏離：課表方塊的上下邊帶

`trip.html` 的行程方塊上下各有一條可以拉時間的邊帶，**它達不到 44px**，這是知情的取捨。

方塊要同時塞下「上邊帶、中間移動區、下邊帶」三個 44px 的目標，高度至少要 132px
（約 88 分鐘）。門檻設在那裡的話，一小時左右的行程就完全不能用拉的，而那是最常見的長度。

現在的做法是 `GRIP_MIN_PX = 88`（手機）、邊帶取方塊高的 1/4 上限 44px，所以：

| 方塊高 | 約幾分 | 邊帶 | 中間 |
|---:|---:|---:|---:|
| 88px | 58 | 22px | 44px |
| 132px | 88 | 33px | 66px |
| 176px | 117 | 44px | 88px |

**中間的移動區永遠 ≥44px**（`h − 2×(h/4) = h/2`），邊帶則是 22px 起跳。
說明書有寫「短的行程沒有邊，改時間請用面板」，面板裡每個操作都有按鈕路徑。
要改回完全合規就是把門檻拉到 132px，代價是一小時的行程拉不動。

### CSS 優先級的坑（踩過三次了）

狀態樣式（`.on`、`.done`）跟 `:active` **優先級相同**，寫在後面的贏：

| 狀態規則 | 優先級 | 按壓規則 | 優先級 | 結果 |
|---|---|---|---|---|
| `.trip-card.done` | (0,2,0) | `.trip-card:active` | (0,2,0) | 平手，`.done` 在後面 → 沒回饋 |
| `.daybar button.on` | (0,2,1) | `.daybar button:active` | (0,2,1) | 同上 |
| `.mini.on` | (0,2,0) | `.mini:active` | (0,2,0) | 同上 |

**處方**：只要狀態樣式設了跟 `:active` 相同的屬性，就一定要補一條
`.<元件>.<狀態>:active`，多一個 class 才壓得過去。不要靠調換順序，那很容易又被改回來。

### CSS 自訂屬性只往下繼承，不會傳給兄弟

`--foo` 定在某個元素上，只有它的**子孫**讀得到，兄弟節點讀不到，而且讀不到時
`background:var(--foo)` 會整個失效變成透明——不會報錯，只是東西消失。

時間軸方塊踩過：分類的 `--tint-*` 掛在 `.tt-b-in` 上，但 `.tt-grip` 是它的兄弟，
結果上下邊帶的橫槓變成 `rgba(0,0,0,0)`，把手整個看不見。

**處方**：一組元件共用的自訂屬性掛在**最外層那個容器**上，不要掛在其中一個子元素上。

按下時怎麼變色：

- **未選中**（淺底）→ 套 `background:var(--accent-soft)`
- **已選中**（實心主色）→ 套 `filter:brightness(.9)`。套淺色底會看起來像被取消選取

---

## 4. 共用元件

這些元件三站長得一樣，改了要一起改。

### 選人畫面

```
標題：你是哪位快樂姊妹？
說明：選擇你的名字，密碼跟運動排程站是同一組。
版面：.people-grid 三欄 × .person-btn 64px 小方塊
```

### 行程與記帳的切換列

```
.switchbar → 兩顆膠囊：行程（pin）／記帳（dollar）
目前所在的那一顆用 --accent-deep 實心白字，並加 aria-current="page"
```

- **只有 `trip.html` 和 `split.html` 有這一排，`index.html` 沒有。**
  旅程與記帳是一組（名單上也是同一個 `canTrip` 開關在管），運動排程站跟它們
  **沒有功能上的關係、只是共用同一份名單**，並排會讓人以為是同一個產品的三個分頁。
  試過三顆，拿掉了。
- **圖示不跟著字級走。** `.switchbar svg` 固定 `17px`。Feather 是照 24px 畫的，
  用 `1em`（13.5px）線寬只剩約 1.1px，細節會糊成一團。
- `<a>` 用 `inline-flex` 置中，靠 `vertical-align` 對不準 17px 配 13.5px 的組合。
- 只出現在**登入後**的畫面，選人與設定密碼的畫面沒有。

### 右上角說明鈕與說明書

- `.corner-btn` 固定右上角，44×44，`z-index: var(--z-corner)`，含 safe-area
- 說明書 `.help` 整頁蓋掉、可捲、Esc 可關、焦點要移進去、有 `aria-label`
- 內容寫法：**名詞 ＋ 一句話**，不要寫成段落。要能用掃的找到資訊
- 每一站的名詞不同，內容各自寫，但版面結構一致

### 清單頁

```
h1.list-title（不要包在只裝一顆按鈕的卡片裡）
  → 卡片清單
  → 「已結束」小標 → 已結束的卡片
  → 建立新◯◯ 按鈕（在清單下方）
  → 回到選擇身份
```

- 卡片**沒有左側色條**
- 「已結束」用 `background:var(--bg)` 區分，加上「已結束」標籤，不靠顏色

### 可以操作的東西不能只是 `<div>`

點得下去就要**按得到**。`<div onclick>` 沒有 `tabindex`、沒有 `role`、沒有 `aria-label`，
用鍵盤的人完全到不了，讀螢幕的人也不知道它可以按。

時間軸的行程方塊踩過：整頁只有它不是 `<button>`，而開始／結束／停留／移除全部都在
「點下去才出現」的面板裡，等於用鍵盤的人看不到也改不了任何已排的行程。

**處方**：能用 `<button>` 就用 `<button>`。需要自己接拖曳手勢而不能用 `<button>` 時，
補齊四件事——`tabindex="0"`、`role="button"`、`aria-label`、Enter 與空白鍵的 `keydown`
（空白鍵記得 `preventDefault()`，否則會捲動頁面）。唯讀／離線時要把 `tabindex` 拿掉，
不要留一個聚焦得到卻沒有反應的東西。

**面板關掉之後焦點要送回原本那個元素**，否則焦點會掉到 `<body>`，鍵盤使用者當場迷路。

### 被截斷的文字要看得到全文

用 `text-overflow:ellipsis` 就要補 `title`（滑鼠停著看得到）。讀螢幕的人走 `aria-label`
或完整的內文，不要讓省略號是唯一的出口。

### 圖示

- 一律內嵌 SVG（Feather 4.29.2，少數取自 Lucide），**不用 emoji、不連 CDN**
- 純圖示的按鈕一定要有 `title` 和 `aria-label`
- 用不到的圖示要從 `ICONS` 拿掉

### 文案

- 短。能三個字講完就不要用一句話
- 空狀態：「還沒有旅程」「還沒有景點」「還沒有帳本」——不要加「建一個吧」
- 不要寫「只看得到自己是成員的⋯⋯」這種解釋文；那種話寫在說明書裡

---

## 5. 版面寬度

| 情境 | 寬度 |
|---|---|
| 預設（含手機） | `.wrap { max-width: 760px }` |
| 旅程內頁（`trip.html`，視窗 ≥1000px） | `.wrap.wide { max-width: 1140px }` |

**寬版屬於「整個旅程內頁」，不屬於某個分頁。** 只在時間軸套用的話，切到景點分頁
時整頁寬度會跳動。`.wide` 的判斷條件是 `view === 'trip'`，不是 `tab === 'timeline'`。

寬版之下：

- 時間軸：`.tl-cols` 左課表、右景點清單
- 景點：`.place-list` 兩欄，否則單張卡片會被拉成長條
  - 兩欄並排時卡片要**鎖成一樣高**：名稱一行、停留一行、備註一行，超出用省略號。
    沒備註的卡片也要留著那一行（`.pc-note:empty` 給 `min-height`），不然會比隔壁矮一截。
    這整組規則只寫在 `@media (min-width:1000px)` 裡——手機是單欄、上下沒得比，
    備註要完整顯示。

---

## 6. 資料共用

- 三站共用同一份 `people-list`（成員與密碼），在運動排程站建立與設定
- **一份名單、分站開放。** 每個人身上兩個欄位決定他在哪幾站看得到：

  | 欄位 | 管哪一站 | 新成員預設 |
  |---|---|---|
  | `canSport` | `index.html` 運動排程（含裡面的姊妹行事曆分頁） | 開 |
  | `canTrip` | `trip.html` 旅程與 `split.html` 記帳，兩站同一個開關 | 關 |
  | `canSis` | 運動排程站裡的「姊妹行事曆」分頁 | 關 |

  **判斷一律寫成 `p.canSport !== false`**（`inSport()` / `inTrip()`），不要寫成
  `!!p.canSport`。改版前建立的成員身上沒有這兩個欄位，用 `!== false` 才會當成全開；
  寫成 `!!` 的話全部的人會在三站同時消失。

- **`people` 永遠是完整名單**，只有下列畫面走過濾後的清單
  （`sportPeople()` / `tripPeople()`）：

  | 過濾 | 不過濾 |
  |---|---|
  | 選人畫面 | 依 id 查名字（`personName()`） |
  | 挑成員的勾選清單 | 既有旅程的 `memberIds`、帳本的分攤 |
  | 老師的統計與備註彙整 | 管理面板 |
  | 記住上次是誰的自動登入 | |

  歷史資料不過濾，否則帳本裡的付款人會變成「(已離開的成員)」。挑成員的清單
  （`memberChoices()`）還要把「已經被選進來、但後來被關掉」的人補回去並勾著，
  不然存一次檔就把人靜靜地踢出旅程。

- **密碼只有運動排程站設得了**。還沒設過密碼的人被關掉 `canSport` 就再也設不了，
  旅遊記帳也跟著登不進去——管理面板關這個開關之前要攔一次。
- 登入邏輯一致：選名字 → 輸入密碼 → 記住上次是誰（`localStorage`，只記 id 不記憑證）
- 離線一律唯讀：`applyReadOnly()` 停用所有沒有 `data-nav` 的控制項
- 每個檔案的 `firebaseConfig` 各自獨立，搬檔案時不能覆蓋

---

## 7. 改動前的自我檢查

動任何共用的東西之前，問自己這四題：

1. **這個東西三站都有嗎？** 有的話三站一起改。
2. **這是分頁的事還是整頁的事？** 分頁切換時不該改變頁面框架（寬度、標頭、位置）。
3. **我改的屬性會不會跟既有的狀態樣式撞優先級？** 尤其 `:active` 與 `.done` / `.on`。
4. **改了顏色的話，對比算過了嗎？** 小字 4.5:1，非文字 3:1。
