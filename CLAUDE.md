# 專案須知

給姊妹們用的三頁式網站，共用同一份成員清單與同一套視覺。

| 檔案 | 內容 |
|---|---|
| `index.html` | 運動排程（成員與密碼的來源） |
| `trip.html` | 旅程行程（課表） |
| `split.html` | 團體記帳 |
| `test/*.html` | 各自的測試複本，只有 `firebaseConfig` 不同 |

## 動手之前

**先讀 `SPEC.md`。** 那裡面是三站共用的規範：設計 token、觸控與無障礙的硬規定、
共用元件、版面寬度、改動流程。

這個專案最常出的錯是**共用的東西只改了一處**——改了行程站忘了記帳站、改了時間軸
忘了景點分頁。`SPEC.md` 第 7 節有四題自我檢查，動共用的東西之前一題一題過。

## 每次改動的固定動作

1. 改 `test/` 底下的檔案，不要直接改正式站
2. 語法檢查：抽出 `<script>` 區塊跑 `node --check`
3. 改到顏色就算對比（小字 4.5:1）；改到可點元素就確認 44px
4. 推測試站 → 使用者驗證 → 搬正式站
5. 搬正式站時保留原本的 `firebaseConfig`，搬完確認
   `grep -c 'weekly-sport-test'` 為 `0`、`diff` 差異為 14 行

## 使用者的偏好

- **不用 emoji**，圖示一律內嵌 SVG（離線也要畫得出來）
- 文案要短。能三個字講完就不要寫一句話
- 說明性的長句放進說明書，不要放在操作畫面上
- 主要使用情境是**手機**；桌機的優化不能犧牲手機
